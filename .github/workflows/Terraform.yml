name: Terraform
on: [pull_request]
jobs:
   WhateverName:
    name: Terraform
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v1.0.0

    - name: Login to Azure
      run: |
        az login -u ${{ secrets.AZ_DEV_UNAME}} -p lololol1231

    - name: Terraform - Init
      uses: hashicorp/terraform-github-actions/init@v0.3.6

    - name: Terraform - Validate
      uses: hashicorp/terraform-github-actions/validate@v0.3.6
 
    - name: Terraform - Plan
      uses: hashicorp/terraform-github-actions/plan@v0.3.6
   
